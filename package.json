{
  "name": "pikachu",
  "version": "1.0.0",
  "description": "用css画一个皮卡丘",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel ./src/index.html --no-cache",
    "build": "parcel build ./src/index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url /pikachu",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [
    "css",
    "皮卡丘"
  ],
  "author": "Leo",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {}
}
